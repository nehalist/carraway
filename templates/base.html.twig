<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Carraway{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}"/>
</head>
<body>
{% if app.user %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="{{ url('dashboard') }}">Carraway</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item {{ app.request.get('_route') == 'dashboard' ? 'active' }}">
                        <a class="nav-link" href="{{ url('dashboard') }}">Dashboard</a>
                    </li>
                    <li class="nav-item {{ app.request.get('_route') == 'contactRequestsOverview' ? 'active' }}">
                        <a class="nav-link" href="{{ url('contactRequestsOverview') }}">Contact Requests</a>
                    </li>
                    <li class="nav-item {{ app.request.get('_route') == 'subscriptionOverview' ? 'active' }}">
                        <a class="nav-link" href="{{ url('subscriptionOverview') }}">Subscriptions</a>
                    </li>
                </ul>

                <ul class="navbar-nav">
                    <li class="navbar-text">
                        Logged in as {{ app.user.username }}
                    </li>
                    <li class="nav-item">
                        <a href="{{ url('logout') }}" class="nav-link">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endif %}

<div class="container" style="margin-top: 20px;">
    {% block body %}{% endblock %}
</div>

<footer class="container text-right mt-5 p-3 border-top">
    <a href="https://github.com/nehalist/carraway" target="_blank">Carraway</a> by <strong><a href="https://nehalist.io" target="_blank">nehalist.io</a></strong>
</footer>
</body>
</html>
